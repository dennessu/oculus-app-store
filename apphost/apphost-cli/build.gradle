apply plugin: 'groovy'
apply plugin: 'application'

mainClassName = "com.junbo.apphost.core.JunboApplication"

dependencies {
    compile project(':apphost-core')
    compile (libraries.common_configuration_data)

    compile "com.junbo.identity:identity-rest:${junboVersion}"
    compile "com.junbo.oauth:oauth-api:${junboVersion}"

    // more to add
}
