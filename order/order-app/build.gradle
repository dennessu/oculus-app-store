apply plugin: 'groovy'
apply plugin: 'application'

mainClassName = "com.junbo.apphost.core.JunboApplication"

dependencies {
    runtime (libraries.apphost_core)
    runtime (libraries.common_configuration_data)

    compile project(':order-rest')
    compile project(':order-jobs')

    runtime "com.junbo.billing:billing-rest:${junboVersion}"
    runtime "com.junbo.identity:identity-rest:${junboVersion}"
    runtime "com.junbo.payment:payment-rest:${junboVersion}"
    runtime "com.junbo.rating:rating-rest:${junboVersion}"
    runtime "com.junbo.catalog:catalog-rest:${junboVersion}"
}
