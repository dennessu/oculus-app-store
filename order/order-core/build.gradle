apply plugin: 'groovy'

dependencies {
    compile junboProject(":order-spec")
    compile junboProject(":order-auth")
    compile junboProject(":order-clientproxy")
    compile junboProject(":order-db")
    compile (libraries.aspectjrt)
    compile (libraries.aspectjweaver)
    testCompile (libraries.spring_test)
    testCompile (libraries.easy_mock)
}
