apply plugin: 'groovy'

dependencies {
    compile junboProject(":order-spec")
    compile junboProject(":order-core")
    compile junboProject(":common-configuration")
    compile (libraries.quartz_scheduler)

    testCompile (libraries.spring_test)
    testCompile (libraries.common_lang)
    testRuntime (libraries.el_api)
    testRuntime (libraries.el)
    testCompile (libraries.easy_mock)
}