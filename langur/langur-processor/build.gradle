apply plugin: 'groovy'

dependencies {
    compile junboProject(':langur-core')

    compile (libraries.rs_api)
    compile (libraries.freemarker)


    testCompile (libraries.slf4j_api)
    testCompile (libraries.spring_beans)
    testCompile (libraries.spring_context)

    testRuntime (junboProject(':common-lib')) {
        exclude group: 'com.junbo.langur', module: 'langur-core'
    }
}
