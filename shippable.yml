language: python

cache: true

jdk:
   - oraclejdk7

services:
  - couchdb
  - memcached

addons:
  postgresql : "9.3"

branches:
  only:
    - release

before_script:
  - ./setupdb.sh

script:
  - ./setupdb.sh

notifications:
  email:
   recipients:
     - txchen@silkcloud.com
   on_success: change
   on_failure: always
