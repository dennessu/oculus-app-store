apply plugin: 'groovy'

dependencies {
    compile project(':billing-rest')

    compile (libraries.jersey_container_grizzly2_http)
    compile (libraries.jersey_spring3)

    runtime (libraries.common_configuration_data)
}