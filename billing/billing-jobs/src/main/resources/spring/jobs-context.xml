<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.junbo.billing.db.*"/>

    <bean id="billingBalanceClient" class="com.junbo.billing.spec.resource.proxy.BalanceResourceClientProxy"
          parent="baseClientProxy">
        <property name="target" value="${clientproxy.billing.url}"/>
    </bean>

    <bean id="billingBillingFacade" class="com.junbo.billing.jobs.clientproxy.BillingFacadeImpl" />

    <bean id="billingOrderEventClient" class="com.junbo.order.spec.resource.proxy.OrderEventResourceClientProxy"
          parent="baseClientProxy">
        <property name="target" value="${clientproxy.order.url}"/>
    </bean>

    <bean id="billingOrderFacade" class="com.junbo.billing.jobs.clientproxy.OrderFacadeImpl" />

    <bean id="asyncChargeListener" class="com.junbo.billing.jobs.asyncCharge.AsyncChargeListener" />

    <bean id="billingListenerContainer" class="org.springframework.jms.listener.DefaultMessageListenerContainer" parent="baseListenerContainer">
        <property name="destination" ref="billingAsyncChargeQueue" />
        <property name="messageListener">
            <bean class="com.junbo.apphost.core.LazyInitProxyFactoryBean">
                <property name="targetBeanName">
                    <idref bean="asyncChargeListener"/>
                </property>
            </bean>
        </property>
    </bean>

    <!-- Billing Scheduler -->
    <bean id="billingSchedulerFactoryBean" class="org.springframework.scheduling.quartz.SchedulerFactoryBean"
          parent="baseJobScheduler">
        <property name="schedulerName" value="billingScheduler" />
        <property name="autoStartup" value="${billing.jobs.scheduler.start}" />
        <property name="triggers">
            <list>
                <ref bean="billingSettlementJobTrigger"/>
            </list>
        </property>
    </bean>

    <!-- Billing Balance Process Job Trigger -->
    <bean id="billingSettlementJobTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean" >
        <property name="jobDetail" ref="billingSettlementJobDetail" />
        <property name="cronExpression" value="${billing.jobs.settlement.cronexpression}" />
    </bean>

    <!-- Billing Balance Process Job -->
    <bean id="billingSettlementJobDetail" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject">
            <bean class="com.junbo.apphost.core.LazyInitProxyFactoryBean">
                <property name="targetBeanName">
                    <idref bean="billingSettlementJob"/>
                </property>
            </bean>
        </property>
        <property name="targetMethod" value="execute" />
    </bean>

    <bean id="billingSettlementJob" class="com.junbo.billing.jobs.settlement.SettlementJob" />

    <bean id="settlementProcessor" class="com.junbo.billing.jobs.settlement.SettlementProcessor">
        <property name="concurrentCount" value="${billing.jobs.settlement.concurrentCount}" />
        <property name="processCount" value="${billing.jobs.settlement.processCount}" />
        <property name="timeLimitMinutes" value="${billing.jobs.settlement.timeLimitMinutes}" />
        <property name="transactionManager" ref="transactionManager" />
    </bean>

</beans>