ext {
    set("current_version", "0.0.1-SNAPSHOT")
    set("artifactory_contextUrl", "https://arti.silkcloud.info:8444/artifactory")
    set("artifactory_user", "wansan-dev")
    set("artifactory_password", "Welcome123")
}
