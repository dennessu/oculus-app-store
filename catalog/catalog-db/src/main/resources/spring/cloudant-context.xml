<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- Repository -->
    <bean id="itemRevisionCloudantRepo" class="com.junbo.catalog.db.repo.impl.cloudant.ItemRevisionRepositoryImpl" parent="cloudantClient">
        <property name="idGenerator" ref="oculus48IdGenerator"/>
        <property name="dbName" value="item_revision"/>
        <property name="cloudantDBUri" value="${catalog.cloudant.url}" />
        <property name="cloudantUser" value="${catalog.cloudant.user}" />
        <property name="cloudantPassword" value="${catalog.cloudant.password}" />
    </bean>
    <bean id="itemAttributeCloudantRepo" class="com.junbo.catalog.db.repo.impl.cloudant.ItemAttributeRepositoryImpl" parent="cloudantClient">
        <property name="idGenerator" ref="oculus48IdGenerator"/>
        <property name="dbName" value="item_attribute"/>
        <property name="cloudantDBUri" value="${catalog.cloudant.url}" />
        <property name="cloudantUser" value="${catalog.cloudant.user}" />
        <property name="cloudantPassword" value="${catalog.cloudant.password}" />
    </bean>
</beans>
