apply plugin: 'java'

dependencies {
    compile junboProject(":rating-common")
    compile junboProject(":rating-spec")
    compile junboProject(':rating-clientproxy')

    testCompile (libraries.spring_test)
}
