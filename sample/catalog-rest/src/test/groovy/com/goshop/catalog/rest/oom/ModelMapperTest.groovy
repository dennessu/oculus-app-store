package com.goshop.catalog.rest.oom


import org.testng.annotations.Test


/**
 * Created by kevingu on 11/20/13.
 */
class ModelMapperTest {

    @Test
    void test1() {
/*

        ModelMapper mapper = new ModelMapperImpl(
                __builtinMapper: new BuiltinMapper()
        )

        def mappingContext = new MappingContext(
                propertyMappingFilter: new PropertyMappingFilterImpl(),
                propertiesToInclude: ["categories", "categories.storeId", "storeId"],

                elementMappingFilter: new AttributeFilter()
        )

        def categoryDTO1 = new ECategory(
                name: "name1",
                storeId: "storeId1",
                description: "description1"
        )

        def categoryDTO2 = new ECategory(
                name: "name2",
                storeId: "storeId2",
                description: "description2"
        )

        def categoryDTO3 = new ECategory(
                name: "name2",
                storeId: "storeId2",
                description: "description2"
        )

        def productDTO = new EProduct(
                name: "product1",
                categories: [categoryDTO1, categoryDTO2, categoryDTO3]
        )


        def categories = mapper.exportCategories(productDTO, mappingContext)

        println(categories)*/
    }
}
