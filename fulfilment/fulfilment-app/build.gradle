apply plugin: 'groovy'
apply plugin: 'application'

mainClassName = "com.junbo.apphost.core.JunboApplication"

dependencies {
    runtime (libraries.apphost_core)
    runtime (libraries.common_configuration_data)

    compile project(':fulfilment-rest')
    compile 'com.junbo.entitlement:entitlement-rest:0.0.1-SNAPSHOT'
    compile 'com.junbo.catalog:catalog-rest:0.0.1-SNAPSHOT'

    compile 'com.junbo.notification:notification-core:0.0.1-SNAPSHOT'

    runtime (libraries.common_configuration_data)

    testCompile (libraries.spring_test)
}
