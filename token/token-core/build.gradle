apply plugin: 'java'

dependencies {
    compile project(":token-db")
    compile project(":token-spec")
    compile project(":token-clientproxy")

	
	testCompile(libraries.spring_test)
    testCompile(libraries.common_configuration_data)
}
