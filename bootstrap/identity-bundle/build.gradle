apply plugin: 'application'
apply plugin: 'groovy'

mainClassName = "com.junbo.bootstrap.IdentityMain"

dependencies {
    compile "com.junbo.identity:identity-app:${junboVersion}"
    compile "com.junbo.oauth:oauth-app:${junboVersion}"

    compile (libraries.common_lib)
    compile (libraries.common_configuration)
    compile (libraries.common_configuration_data)
    compile (libraries.common_sharding)
}