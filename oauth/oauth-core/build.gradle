apply plugin: 'groovy'

dependencies {
    compile(libraries.spring_aop)
    compile(libraries.spring_context)
    compile(libraries.spring_core)
    compile(libraries.spring_expression)
    compile(libraries.spring_tx)
    compile(libraries.spring_jdbc)
    compile(libraries.spring_orm)
    compile(libraries.spring_beans)
    compile(libraries.spring_web)
    compile(libraries.spring_security)

    compile(libraries.jersey_server)

    compile junboProject(':silkcloud-core')
    compile junboProject(':authorization-lib')

    compile ("com.nimbusds:nimbus-jose-jwt:2.22.1")

    compile junboProject(':oauth-spec')
    compile junboProject(':store-spec')
    compile junboProject(':oauth-db')
    compile junboProject(':oauth-clientproxy')
}
