apply plugin: 'groovy'

dependencies {
    compile (libraries.gson)
    compile (libraries.common_codec)
    compile (libraries.hamcrest_core)

    compile junboProject(':identity-data')
    compile junboProject(':identity-spec')
    compile junboProject(':authorization-lib')
}
