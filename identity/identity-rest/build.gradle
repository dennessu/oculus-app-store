import com.sun.org.apache.regexp.internal.recompile

apply plugin: 'groovy'

dependencies {
    compile (libraries.servlet_api)
    // compile (libraries.jersey_spring3)
    compile (libraries.jackson_jaxrs_json_provider)
    compile (libraries.jackson_annotations)
    compile (libraries.common_beanutils)

    compile (libraries.spring_aop)
    compile (libraries.spring_context)
    compile (libraries.spring_core)
    compile (libraries.spring_expression)
    compile (libraries.spring_tx)
    compile (libraries.spring_web)
    compile (libraries.common_lib)

    compile (libraries.asyncclient)
    compile (libraries.oom_processor)

    compile (libraries.oom)
    compile (libraries.jersey_common)
    compile ('com.junbo.authorization:authorization-lib:0.0.1-SNAPSHOT')
    compile('com.junbo.oauth:oauth-spec:0.0.1-SNAPSHOT')

    compile project(':identity-core')
    compile project(':identity-spec')
    compile project(':identity-common')
    compile project(':identity-data')
    compile project(':identity-clientproxy')
}