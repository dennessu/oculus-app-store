apply plugin: 'groovy'

dependencies {
    compile (libraries.apache_common_io)

    compile junboProject(':crypto-common')
    compile junboProject(':crypto-data')

    compile junboProject(':crypto-clientproxy')

    compile junboProject(':identity-spec')

    testCompile (libraries.spring_test)
}
