<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       ">
    <import resource="transaction.xml"/>

    <bean id="configDataSource" class="bitronix.tm.resource.jdbc.PoolingDataSource"
          init-method="init" destroy-method="close">
        <property name="uniqueName" value="jdbc/config"/>
        <property name="className" value="${config.db.driverClassName}"/>
        <property name="minPoolSize" value="${config.db.minConnections}"/>
        <property name="maxPoolSize" value="${config.db.maxConnections}"/>
        <property name="driverProperties">
            <props>
                <prop key="url">${config.db.url}</prop>
                <prop key="user">${config.db.username}</prop>
                <prop key="password">${config.db.password}</prop>
            </props>
        </property>
    </bean>

    <bean id="btmDataSourceTemplate" class="bitronix.tm.resource.jdbc.PoolingDataSource" scope="prototype" lazy-init="true">
        <property name="className" value="${sharding.dataSource.driverClassName}"/>
        <property name="minPoolSize" value="${sharding.dataSource.db.minConnections}"/>
        <property name="maxPoolSize" value="${sharding.dataSource.db.maxConnections}"/>
        <property name="ShareTransactionConnections" value="true"/>
        <property name="driverProperties">
            <props>
                <prop key="password">${sharding.dataSource.db.password}</prop>
            </props>
        </property>
    </bean>

    <bean id="shardDataSourceMapper" class="com.junbo.sharding.core.ds.ShardDataSourceMapper">
        <property name="dataSourceMapping" value="${sharding.dataSource.mapping}"/>
    </bean>
    <bean id="indexDataSourceMapper" class="com.junbo.sharding.core.ds.ShardDataSourceMapper">
        <property name="dataSourceMapping" value="${partition.dataSource.mapping}"/>
    </bean>

    <bean id="shardDataSourceFactory" class="com.junbo.sharding.core.ds.ShardDataSourceFactoryImpl">
        <property name="dataSourceBeanName" value="btmDataSourceTemplate"/>
        <property name="mapper" ref="shardDataSourceMapper"/>
    </bean>
    <bean id="indexDataSourceFactory" class="com.junbo.sharding.core.ds.ShardDataSourceFactoryImpl">
        <property name="dataSourceBeanName" value="btmDataSourceTemplate"/>
        <property name="mapper" ref="indexDataSourceMapper"/>
    </bean>

    <bean id="shardDataSourceRegistry" class="com.junbo.sharding.core.ds.ShardDataSourceRegistry">
        <property name="dataSourceFactory" ref="shardDataSourceFactory"/>
    </bean>
    <bean id="indexDataSourceRegistry" class="com.junbo.sharding.core.ds.ShardDataSourceRegistry">
        <property name="dataSourceFactory" ref="indexDataSourceFactory"/>
    </bean>

    <!-- Sharded SessonFactory for Hibernate-->
    <bean id="sessionFactoryTemplate" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="jtaTransactionManager" ref="transactionManager"/>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${sharding.hibernate.dialect}</prop>
                <prop key="hibernate.max_fetch_depth">${sharding.hibernate.max_fetch_depth}</prop>
                <prop key="hibernate.jdbc.fetch_size">${sharding.hibernate.fetch_size}</prop>
                <prop key="hibernate.jdbc.batch_size">${sharding.hibernate.batch_size}</prop>
                <prop key="hibernate.show_sql">${sharding.hibernate.show_sql}</prop>
            </props>
        </property>
    </bean>
</beans>